export default function ({ store, redirect }) {
  // If the user is not authenticated
  // Uncoment this next lines to have autentication
  // if (!store.state.auth) {
  //   return redirect('/pages/login')
  // }
}
